# dva-graphql-mqtt-demo
a dva demo application that supports GraphQL and MQTT

#Installation
```
    npm install --save mqtt
    npm install --save apollo-boost graphql-tag graphql
```

#Notes
'subscription' does NOT supporte yet.
